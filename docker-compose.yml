version: '3.8'

services:
  mock-api:
    build: .
    container_name: mock-price-api
    ports:
      - "5000:5000"
    environment:
      - FLASK_ENV=production
      - PYTHONUNBUFFERED=1
    restart: unless-stopped